# Final GitHub Manual Steps

## Profile README Setup

1. Create a public repo named exactly `Ishan-113`.
2. Copy `GITHUB_PROFILE_README.md` into that repo as `README.md`.
3. Pin repos in this order:

   - `pentest-writeups`
   - `Pentest_cheatsheet`
   - `GoT-Lore-Map`
   - `portfolio`
   - `expense-tracker`
   - `weather-app`
